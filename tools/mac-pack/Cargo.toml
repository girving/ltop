[package]
name = "mac-pack"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "mac-pack"
path = "src/main.rs"

# sha2 for CodeDirectory page hashes. No alloc/std constraints on a
# host tool; pull in the default backend.
[dependencies]
sha2 = "0.10"
